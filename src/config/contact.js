// ============================================================
// CONTACT / FORM CONFIGURATION
// Replace with actual API credentials before going live.
// ============================================================

export const contactConfig = {
  // Web3Forms — get a free key at https://web3forms.com
  // Leave empty to disable form submission (form will log to console in dev)
  web3formsKey: "", // e.g. "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

  // EmailJS (alternative) — leave all blank if not using
  emailjsServiceId: "",
  emailjsTemplateId: "",
  emailjsPublicKey: "",

  // Fallback email address shown when API is not configured
  fallbackEmail: "", // e.g. "info@bestcaraccessories.in"
};

export default contactConfig;
